{
  "name": "newwave-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler versions upload",
    "check": "wrangler types --check && tsc --noEmit && wrangler deploy --dry-run",
    "types": "wrangler types",
    "ruby-os": "python3 RUBY_OS_v4.2.py"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "typescript": "^5.8.3",
    "wrangler": "^4.24.3"
  }
}
